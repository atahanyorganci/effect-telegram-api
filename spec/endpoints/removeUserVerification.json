{
	"kind": "method",
	"name": "removeUserVerification",
	"slug": "removeuserverification",
	"description": "Removes verification from a user who is currently verified on behalf of the organization represented by the bot",
	"parameters": [
		{
			"name": "user_id",
			"type": {
				"kind": "integer"
			},
			"required": true,
			"description": "Unique identifier of the target user"
		}
	],
	"returns": {
		"kind": "literal",
		"value": true
	}
}
