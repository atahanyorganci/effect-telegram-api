{
	"kind": "method",
	"name": "addStickerToSet",
	"slug": "addstickertoset",
	"description": "Use this method to add a new sticker to a set created by the bot. Emoji sticker sets can have up to 200 stickers. Other sticker sets can have up to 120 stickers",
	"parameters": [
		{
			"name": "user_id",
			"type": {
				"kind": "integer"
			},
			"required": true,
			"description": "User identifier of sticker set owner"
		},
		{
			"name": "name",
			"type": {
				"kind": "string"
			},
			"required": true,
			"description": "Sticker set name"
		},
		{
			"name": "sticker",
			"type": {
				"kind": "ref",
				"name": "InputSticker"
			},
			"required": true,
			"description": "A JSON-serialized object with information about the added sticker. If exactly the same sticker had already been added to the set, then the set isn't changed."
		}
	],
	"returns": {
		"kind": "literal",
		"value": true
	}
}
